# Production environment variables (committed to repo)
# NEXT_PUBLIC_* vars are public (baked into client bundle at build time)
NEXT_PUBLIC_BACKEND_URL=/api/backend

# NextAuth v5 — trust the Host header so redirects use the correct domain
# (floorscan.ai in prod, preview URLs for previews — no manual AUTH_URL needed)
AUTH_TRUST_HOST=true
